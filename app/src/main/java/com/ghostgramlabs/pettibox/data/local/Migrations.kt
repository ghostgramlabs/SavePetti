package com.ghostgramlabs.pettibox.data.local

/**
 * Reserved for future schema bumps. We're at v1 (clean baseline) and have no
 * registered migrations yet. When v2 lands, add a [androidx.room.migration.Migration]
 * here and register it in the Hilt database provider.
 */
